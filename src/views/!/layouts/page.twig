{% include 'lk-makeup::!.partials.dispatcher' %}

<div class="makeup--frame" {% if breakpoint %}style="margin:0 auto;width: {{ breakpoint }}px"{% endif %}>
    <iframe src="/makeup/frame-page-{{ page }}?theme={{ theme }}"
            name="site"
            frameborder="0"></iframe>
</div>